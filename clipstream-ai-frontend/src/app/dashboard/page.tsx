// dashboard/page.tsx
// -----------------
// Main dashboard page for Clipstream AI. Currently displays a basic welcome message.
// This page will be expanded to show user's uploaded files, clips, and processing status.

"use server";

// Main dashboard page component
export default async function DashboardPage() {
  return (
    <div>
      <h1>Dashboard</h1>
    </div>
  );
}
